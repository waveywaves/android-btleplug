pluginManagement {
    repositories {
        gradlePluginPortal()
        google()
        mavenCentral()
    }
}

dependencyResolutionManagement {
    // repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()
    }
}

include ':jniutils'
project(':jniutils').projectDir = new File('./jni-utils/java')

include ':droidplug'
project(':droidplug').projectDir = new File('./btleplug/src/droidplug/java')

rootProject.name = "btletest"
include ':app'
