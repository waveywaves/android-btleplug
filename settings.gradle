pluginManagement {
    repositories {
        gradlePluginPortal()
        google()
        mavenCentral()
    }
}
dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()
    }
}

include ':droidplug'
project(':droidplug').projectDir = new File('./btleplug/src/droidplug/java')

include ':jniutils'
project(':jniutils').projectDir = new File('./gedgygedgy/jni-utils-rs/java')

rootProject.name = "btletest"
include ':app'
